node_modules
.next
.DS_Store
*.tsbuildinfo
next-env.d.ts

# Secrets. .env.example is the committed template; anything .local holds real
# values and must never be committed.
.env*.local
.env

# Anything a server ever writes about guests. Nothing does now; never commit it.
.data/

# The static export is a build artefact; the Pages Action rebuilds it.
out/

# QA screenshots.
qa/shots/

# The artifact package is a build product; npm run artifact rebuilds it.
.artifact/
*.artifact.html
